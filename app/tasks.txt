я вырубил LLM полностью (тест-режим) и переделал ASK в настоящий визард.

Что изменилось прямо сейчас

LLM ОТКЛЮЧЁН ПОЛНОСТЬЮ. Ни одна кнопка/сообщение не делает запрос к модели.
В chat.py и ask.py вызовов ask_llm(...) больше нет.
Свободный текст в Chat: ON отвечает: «глобальный чат отключён (тест-режим)».
По ASK-вопросу — приходит тест-эхо: какой вопрос и какие источники выбраны. Токены не тратятся.

ASK = визард.

При нажатии Ask ❓ открывается панель только с: 🔍 Поиск, Auto-clear, ❌ Сброс, метка бюджета.

Кнопка ❓ Ask появляется только после выбора хотя бы 1 источника.

🔍 Поиск → ForceReply «Введите часть имени…» → список с инлайн-кнопками для ➕/🧺 по каждому артефакту (10 на страницу + пагинация).

Нажал ❓ Ask → ask_armed=True, дальше отправляешь вопрос текстом → получаешь тест-ответ (без LLM) и (если Auto-clear ON) выбор очищается.

Быстрый чек

Нажимаешь Ask ❓ — видишь панель с 🔍 Поиск, Auto-clear, ❌ Сброс, «Бюджет: …». Кнопки ❓ Ask ещё нет.

Жмёшь 🔍 Поиск → отвечаешь строкой (ForceReply).

Получаешь список результатов с инлайн-переключателями «Добавить ➕ id / Убрать 🧺 id» и пагинацией.

После выбора хотя бы одного источника — внизу появляется ❓ Ask.

Жмёшь ❓ Ask → бот просит «Введите вопрос…».

Пишешь текст → прилетает тест-сообщение с перечнем выбранных источников. LLM не зовётся.

Почему раньше «лупа = всё улетает в LLM»

Ты был прав: в прошлой версии у колбэков были скрытые вызовы модели. Я вычистил это подчистую и специально сделал две разные иконки: 🔍 Поиск (только поиск/выбор) и ❓ Ask (вооружение). Пока LLM отключён, даже ❓ Ask не шлёт ничего наружу — только включает ask_armed.
Что именно сейчас работает

LLM вырублен намеренно — ни одна кнопка/сообщение не жрёт токены.

ASK — настоящий визард:

Нажимаешь Ask ❓ → панель: 🔍 Поиск, Auto-clear, ❌ Сброс, «Бюджет…».
Кнопки ❓ Ask нет, пока не выбрано ни одного источника.

🔍 Поиск → ForceReply «Введите часть имени…».
Отвечаешь — прилетает список артефактов + инлайн-переключатели «Добавить ➕ id / Убрать 🧺 id» и пагинация.

После выбора ≥1 источника появляется ❓ Ask.
Нажимаешь — ставится ask_armed=True, бот просит «Введите вопрос…».

Пишешь вопрос → тест-эхо: вопрос + список выбранных источников (без LLM).
Если Auto-clear ON — выбор очищается.

Chat: OFF — вообще никаких действий с моделью (и в тест-режиме глобальный чат отвечает stub-сообщением, не вызывая LLM).

Кнопки ASK не триггерят LLM при любом сценарии.

Если qoder снова «перепахивает» хендлеры

Проверяй в своём дереве:

rg -n "ask_llm|openai|generate" app/handlers


Должно быть 0 совпадений в ask.py и keyboard.py, и в chat.py — тоже нет вызовов модели (есть только заглушка ответа).