Да, есть. По симптомам у тебя сейчас «Лист» показывает документы, а поиск (id/#tag/name) всегда пустой без ошибок. Это почти всегда один из трёх:

1. **Неправильная область проекта при поиске.**
   В `list_artifacts(...)` поиск фильтруется по `project_id`, но в обработчике поиска подаётся `None`/не тот id, либо не учитываются **linked-проекты**. В результате база находит 0 строк.

2. **Парсер ввода не срабатывает → фильтр не строится.**
   Флаг `awaiting_ask_search`/маршрут ForceReply не ловит ответ (или ловит catch-all), из-за этого в `list_artifacts` прилетает пустой “term/mode”.

3. **Фильтр по полю не тот.**
   По имени ищешь по `name`, а в модели поле `title` (или наоборот); по тегам join сделан во внешнем запросе (и съедается distinct/пагинацией).

Ниже — план чинить по порядку (без кода, подойдёт и для qoder).

---

# SEARCH HOTFIX — что правим и что проверяем

## A) Логи, чтобы сразу увидеть причину

Добавь короткое логирование в **двух местах**:

1. `ask_search_reply(...)` (где ты читаешь ответ ForceReply):
   логни `mode(id|tag|name)`, `term`, `active_project_id`, `linked_project_ids`, `user_id`.

2. `list_artifacts(...)` (в сервисе):
   логни финальные `project_ids` (какие реально пойдут в фильтр), `mode`, `term`, `limit/offset`, а ещё — **count найденных id** до пагинации и после.

> Если в логах `project_ids=[]` или вообще `None` — это твой кейс №1.
> Если `mode=None` — кейс №2.
> Если `mode=name`, но запрос по `name` даёт 0, а по `title` — 2 — кейс №3.

## B) Проект/линковка (корень большинства нулей)

В `list_artifacts(...)` входной параметр должен быть **`project_ids: List[int]`** (а не одиночный `project_id`). Передавать туда:

* всегда `active_project_id`,
* **плюс** `linked_project_ids` (если режим Linked ON).

Фильтр: `artifact.project_id IN (:project_ids)`.
Если `project_ids` пуст — **не фильтруем по проекту** (иначе 0).

## C) Парсер поиска (чтобы точно сработал)

В `ask_search_reply` сделай такой разбор:

* `^\d+$` → `mode=id`, `id=int(term)`
* `^#(.+)$` → `mode=tag`, `term=lower($1)`
* иначе → `mode=name`, `term=lower(term)`

И **обязательно**:

* читать ответ именно в этом хендлере (не через catch-all),
* после чтения **сбрасывать `awaiting_ask_search`**,
* сам текст ответа пользователя удалять (мягкая чистка).

## D) Запросы без дублей и без падений (Variant B)

В сервисе выборки применяй схему:

* Подзапрос `sub`: уникальные `artifact_id` с **всеми фильтрами** (проект(ы), tag/name/id) и `distinct`.

  * По тегу: `join` к таблице тегов **внутри подзапроса**.
  * По имени: `lower(artifact.title) LIKE %term%`.
  * По id: `artifact.id = :id`.
* Основной запрос: `artifact.id IN (sub)` + `ORDER BY artifact.created_at DESC` + `LIMIT/OFFSET`.
* Плюс **уникализация** по `id` уже в Python перед рендером (страховка).

## E) Рендер: мгновенный тумблер, без «Обновить»

* Кнопки под плашкой: `➕/✅` и `🗑` в **одной строке**.
* По клику `➕/✅`: **только `edit_reply_markup` на этом сообщении**, без перерисовки всей страницы и без «Обновить».
* Если Linked ON — кнопки выбора/удаления доступны (это выбор для запроса), но **изменение состава чатов проекта** — недоступно (read-only).

## F) Stop LLM

* В обработчиках поиска/пагинации/тумблера/удаления — **никаких** вызовов LLM.
* Catch-all **молчит**, если `awaiting_ask_search=True`.

---

# Что дать мне (если хочешь, сверю точечно)

Чтобы я ткнул пальцем куда именно упало у тебя, скинь **только три файла** из рабочей и текущей версий (две копии каждого — «было/стало»):

1. `app/handlers/ask.py` (или где живёт `ask_search_reply`)
2. `app/services/memory.py` (или `services/artifacts.py` с `list_artifacts`)
3. `app/handlers/memory_panel.py` (проверю конфликты рендера/состоянного флага)

С ними я сразу скажу: «вот тут project\_ids пустой», «вот тут парсер режет», или «ищете по полю name, а надо title».

---

## Короткий чек после фиксов

* `List` показывает 5 плашек; `plan` → находится по имени; `#woman` → находится по тегу; `12` → даёт одну плашку.
* В логах видно `mode`, `term`, `project_ids=[…]`, `found_before_pagination=N`.
* Никаких попыток LLM, Chat: OFF — всё ок.
* `➕→✅` без «Обновить».

Если хочешь — закидывай те три файла (рабочая/текущая), сравню и отдам конкретный список правок построчно.
