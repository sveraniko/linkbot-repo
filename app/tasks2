ниже — коротко и только по двум пунктам, как просил.

# 1) Refine — как должно работать на практике

* **Цель:** дать возможность «подтюнить» вопрос, видя и свой исходный вопрос, и ответ, без удаления чего-либо.
* **Что хочет пользователь:** при нажатии `🔁 Refine` текст исходного вопроса **попадает в поле ввода**, чтобы его можно было подправить.
* **Техническая реальность Telegram:** боты **не могут** программно «вставить» текст в поле ввода пользователя. В Bot API нет способа предварительно заполнить инпут.
  👉 Поэтому буквально «автозаполнить» не получится ни у кого (ни qoder, ни grok), это ограничение клиента.

### Как делаем, чтобы UX был максимально близок

1. При нажатии `🔁 Refine`:

   * Переходим в режим уточнения и показываем **ForceReply** с **placeholder** = первая строка исходного вопроса (показывается серым в инпуте; пользователь видит, что редактирует именно этот запрос).

     > Плейсхолдер не копируется автоматически — это ограничение Telegram — но визуально помогает.
   * Подсказка в всплывашке: «Введи полный обновлённый вопрос или короткое уточнение».

2. Серверная логика (без «мусорных» сообщений):

   * Если пользователь ввёл **полный новый вопрос** — используем его как новый prompt.
   * Если пользователь ввёл **только уточнение** (короткая фраза) — собираем новый prompt как:
     `исходный_вопрос + "\nУточнение: " + уточнение`
     (то есть мы сами «слипаем» исходный + дельту).

3. Чистота чата:

   * **Не удаляем** исходный вопрос и текущий ответ (они остаются видимыми).
   * «Готовлю ответ…» → редактируем **то же** сообщение с ответом на новый (рефайн-ран). Панель иконок остаётся.

4. Подтверждение для пользователя (без новых сообщений):

   * После получения уточнения даём короткую всплывашку: «Ок, уточняю».
   * Никаких дополнительных «черновиков», «копий вопроса» и пр. в чат **не добавляем**.

**Acceptance для Refine:**

* Нажал `🔁` → вижу инпут с плейсхолдером (кусочек старого вопроса).
* Могу ввести либо новый полный вопрос, либо короткое уточнение.
* Исходный вопрос/ответ на экране **остаются**; новый ответ приходит в **том же** сообщении с иконками.

> Если **критически важно** именно автоподстановка текста в инпут (не placeholder), то это **невозможно** в Telegram Bot API. Единственный путь — кастомный клиент/веб-интерфейс вне Telegram.

---

# 2) Инфраструктурные правила (ENV/Compose — не трогать) и как запускать

**Жёсткие запреты при любых PR/правках:**

* **НЕЛЬЗЯ менять**: `docker-compose.yml`, `docker-compose.override.yml`, `.env`, `.env.local`, `.env.production`, `.env.sample`.
* **НЕЛЬЗЯ добавлять** новые compose-файлы или генерировать альтернативные `.env.*` без письменного решения.
* **НЕЛЬЗЯ** переименовывать/удалять существующие ENV-ключи (бот-токены, ключи LLM, БД и т. п.).

**Почему ломается, когда боты «трогают» конфиги:**

* Они пытаются запускать из локальной консоли, ловят ошибки зависимостей/сетей и «чинят» это правками Compose/ENV, в процессе стирая ключи и разъезжая миграции. Это недопустимо.

**Как правильно запускать (Single Source of Truth):**

1. **Только внутри контейнеров**:

   * сборка/поднятие: `docker compose up -d --build`
   * миграции: `docker compose exec app alembic upgrade head`
   * локальные команды: `docker compose exec app bash` → внутри `poetry run python -m app.main` (или твоя entry-команда)
2. **Секреты и переменные**:

   * ключи и токены — **только** через `.env` (или секреты окружения CI), форматы уже прописаны.
   * если нужен **новый ENV-ключ**, он сначала добавляется в `.env.sample` + документацию, **не** меняя существующие файлы, и используется кодом **только** после ревью.
3. **Никаких «альтернативных способов запуска»** для ИИ-моделей:

   * Все провайдеры (OpenAI/Groq/и т. д.) вызываются из одного сервиса в контейнере.
   * Любые локальные обёртки/скрипты не должны менять пайплайн запуска.

**Acceptance для инфраструктуры:**

* После правок код компилируется и стартует прежней командой `docker compose up -d --build`.
* Все секреты на месте; `.env` не изменён/не перегенерирован.
* В CI/CD шаги те же; миграции применяются стандартно командой внутри контейнера.

---

Если ок, эти два пункта вставим как отдельные разделы в общий документ:
**(Refine UX с учётом ограничений Telegram)** и **(Правила инфраструктуры: ENV/Compose immutable + как запускать)**.
ниже — конкретные предложения, как «пересобрать» ASK по **Антихрупкость v3** так, чтобы он не падал, не расползался и переживал любые частичные правки.

# 1) Архитектура модульная (границы и контракты)

* **Handlers только «склеивают» UI↔сервисы.**
  `app/handlers/ask.py` = thin controller: парсит callback, валидирует стейт, зовёт сервис → рендерит ответ.
* **Логика в сервисах, без Telegram-зависимостей.**
  `app/services/ask_list.py`, `ask_answer.py`, `ask_selection.py`, `llm_pipeline.py`, `artifacts.py`.
* **Строгие неймспейсы callback’ов:**
  `ask:*` (дом/лист/поиск/пагинация/сброс/автоклир) и `ask:answer:*` (save/pin/summary/delete/refine/sources/back/open).
  Никаких `template:*`, `menu:*` внутри ASK.
* **Порядок роутеров:** ASK → остальные панели → catch-all.
  Это «щит» от коллизий вида «✅ Шаблон выбран…».

# 2) Схема стейта (валидация и совместимость)

* Единый dataclass/схема (валидация при чтении):
  `chat_on: bool`, `selected_artifact_ids: list[int]`,
  `ask_page, ask_total_pages, ask_page_msg_ids[], ask_footer_msg_id, ask_prompt_msg_id`,
  `last_answer{run_id, question_msg_id, answer_msg_id, source_ids[], saved, pinned}`,
  `auto_clear_selection: bool`.
* **Версионирование стейта:** `state_version`. При несовпадении — миграция стейта по месту (без падения).
* **Read-only защита** для linked-проектов: флаг в стейте/проекте; сервисы выбора/удаления источников проверяют и возвращают toast вместо действия.

# 3) Неубиваемая клавиатура под ответом

* **Правило-invariant:** любой `edit_message_text` для сообщения ответа **обязан** прикладывать `reply_markup=actions_keyboard(...)`.
  Нарушение = автоматический «снятый» инлайн.
* `📚`/`↩️` меняют **только** клавиатуру (`edit_reply_markup`), текст ответа не трогают.
* Все кнопки работают без новых сообщений: лишь всплывашки/перекладка.

# 4) Идемпотентность и защита от «дабл-тапов»

* Любое действие с побочкой (save/pin/summary/delete/refine) — **idempotent**:
  повторный клик возвращает корректный текущий статус (например «Уже сохранено»).
* **Debounce 1–2 секунды** по run\_id/answer\_msg\_id: повторные клики/дубликаты апдейтов игнорируются.
* **Lock** уровня пользователя для критичных участков (save→pin→summary), чтобы не было гонок.

# 5) Гейт LLM и режимы отказа

* LLM вызывается только из `Ask/Refine` при `chat_on=True` и `selected_artifact_ids != []`.
  Остальные ходы — строго без LLM.
* **Feature-flag’и (ENV, но неизменяемые в коде):**
  `LLM_DISABLED`, `ANSWER_BAR_WRAP (auto|1|2)`, `ANSWER_BAR_ICONS_ONLY (1|0)`, `ASK_PAGE_SIZE (5)`.
  Код читает, но не пишет ENV. Compose/.env — **immutable** (см. ниже).
* **Пониженные режимы:** если LLM недоступен → понятный toast «LLM временно отключён», дерево действий не ломаем.

# 6) Контракты сервисов (узкие, проверяемые)

* `list_sources(project_ids, term, mode, limit, offset) -> (items, total)`: без Telegram; **distinct по сабквери**, стабильная сортировка.
* `toggle_selection(user_id, source_id) -> bool added`: без side-effects в БД.
* `delete_source(source_id) -> ok`: уважает read-only.
* `run_llm(question, source_ids) -> Answer(run_id, text_md, used_source_ids, meta)`.
* `answers.save(run_id, text_md, used_source_ids) -> answer_id`
  `answers.pin(answer_id, flag)`, `answers.add_summary(answer_id, text_md)`, `answers.delete(answer_id)`.

# 7) Ошибки = контролируемые статусы, не падения

* Свои исключения: `UserError` (toast), `ExpectedFailure` (тихий fallback), `InfraError` (лог + «Временно недоступно»).
* Все handlers — `try/except` → перевод в toast/перекладку; **никаких tracebacks в чат**.

# 8) Чистота чата (инварианты)

* Команды/названия кнопок не эхоим никогда.
* Видимые «вечные» сущности: **вопрос пользователя** и **сообщение с ответом** (до Delete).
* Всё остальное — редактирование/перекладка, всплывашки.
* TTL-почистка служебных промптов (ForceReply) — тихо.

# 9) Миграции и схемы данных

* Новые поля только вперёд, **старые ревизии не правим**.
* Артефакт ответа: `kind='answer'` или тег `answer`, `project_id` обязателен, `related_source_ids[]`, `run_meta{model,tokens_in/out,duration}`, `pinned`.
* `Open` после `Save` — открывает карточку артефакта (просмотр без Telegram-завязки).

# 10) Телеметрия и диагностика

* Логируем: `user_id`, `chat_id`, `action`, `callback`, `run_id`, `duration_ms`, `tokens_in/out`, `errors_count`.
* Метрические счётчики по кнопкам (save/pin/summary/delete/refine/sources) → видно, где ломается.

# 11) Тест-матрица (минимум)

* Стейт-миграция vN→vN+1 (пустые/частичные ключи).
* Лист/поиск/пагинация (по id/#tag/name), 0/полная/последняя страница.
* Toggle быстрый (двойной клик), delete read-only.
* Ask при Chat OFF/ON, пустые источники, LLM off, LLM error.
* Answer-бар: `📚`/`↩️`, `💾` idempotent, `📌` toggle, `🧾` append, `🔁` same-message edit, `🗑` confirm/cancel/confirm.
* Всегда: клавиатура под ответом не исчезает.

# 12) Инфраструктурные «нельзя» + как запускать

* **НЕЛЬЗЯ** менять `.env*`, `docker-compose*.yml`, добавлять новые compose/ENV.
* Запуск только через контейнеры:
  `docker compose up -d --build` → `docker compose exec app alembic upgrade head` → `docker compose exec app bash` → `poetry run …`.
* Новый ENV-ключ — только через PR в `.env.sample` + доку. Никаких «локальных альтернатив».

# 13) Гайд-шаблон для любых ИИ-моделей (вставлять в задачу)

* «Вносить правки **только** в перечисленные файлы и только в пределах указанных функций.
  Переименования/переносы модулей — запрещены.
  ENV/Compose — **не трогать**.
  Любая правка проходит чек-лист:

  1. Вопрос пользователя остаётся; 2) Ответ редактируется в том же сообщении; 3) Клавиатура всегда прикладывается; 4) LLM не трогается вне Ask/Refine; 5) `ask:*`/`ask:answer:*` префиксы не коллидируют; 6) Нет новых служебных сообщений».

---

## План внедрения (быстро и безопасно)

1. **Срезать жир**: вынести код из `ask.py` в сервисы (п.6), оставить тонкий слой хендлеров.
2. **Встроить инварианты**: helper-обёртки `edit_with_actions_kbd(...)`, `toast(...)`, `safe_delete(...)`, единая генерация клавиатуры.
3. **Привести префиксы и роутинг** к п.1/2 (гарантия от коллизий).
4. **Валидатор стейта**: при каждом входе в handler нормализовать структуру (default’ы, версия).
5. **Idempotency/locks** в сервисах ответов; дебаунс кликов.
6. **Тест-матрица** (минимум из п.11) — руками/авто.

Это даст «каркас», в который можно смело добавлять фичи — даже если внешняя модель внесёт спорную правку, инварианты/валидация/идемпотентность удержат систему от развала.
